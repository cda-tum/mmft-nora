# Core scientific libraries
numpy>=2.1.1
matplotlib>=3.9
networkx>=3.4.2
ezdxf>=1.3.5
shapely>=2.0

# Backend API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
python-multipart>=0.0.9

# Test tooling
# Newer pytest releases import readline during startup, which crashes with the
# current macOS/Conda Python build used by the local backend venv.
pytest>=7.4,<8
